Положи сюда `track.mp3` — и `node scripts/build-artifact.mjs` соберёт демо
с настоящим звуком вместо Web Audio заглушки.

Файл до ~10 МБ: в артефакте он зашивается в страницу как data: URI,
base64 добавляет примерно треть, а лимит страницы — 16 МБ.
